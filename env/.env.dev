# 本地开发环境配置
VITE_PORT = 80
VITE_BASE_URL = './'

# 后端接口地址（本地开发建议使用 /dev-api，通过 Vite 代理转发）
VITE_APP_BASE_API = '/dev-api'

# 开发代理目标（指向你的 RuoYi 后端）
VITE_PROXY_TARGET = 'http://localhost:8088'

# 兼容旧版本变量（Deprecated）
# VITE_API_DOMAIN = '/dev-api'
